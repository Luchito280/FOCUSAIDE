document.addEventListener('DOMContentLoaded', () => {
    // Inicializar todos los módulos de la interfaz
    UI.init();
    Timer.init();
    Tasks.init();
    Assistant.init();

    console.log("FOCUSAIDE UI inicializada.");
});